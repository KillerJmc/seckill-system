server:
  port: 9041

spring:
  application:
    name: seckill-mock-service
  datasource:
    url: jdbc:mysql:///seckill_mock_db
    username: root
    password: plf,.123

management:
  endpoints:
    web:
      exposure:
        include: '*'
