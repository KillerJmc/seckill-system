server:
  port: 9001

spring:
  application:
    name: seckill-service
  datasource:
    url: jdbc:mysql:///seckill_system
    username: root
    password: root

management:
  endpoints:
    web:
      exposure:
        include: '*'
